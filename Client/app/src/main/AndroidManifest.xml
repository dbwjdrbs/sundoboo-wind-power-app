<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- NOTE : 카메라가 없어도 상관없다라는 부분 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" /> <!-- NOTE : 카메라 권한 선언 -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- NOTE : GPS와 네트워크 권한 선언 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- NOTE : 기기의 대략적인 위치 파악 권한 선언 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Client"
        tools:targetApi="31">
        <activity
            android:name="com.example.sundoboo.common.BusinessSelectActivity"
            android:exported="true" />
        <activity
            android:name="com.example.sundoboo.common.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_start"
            android:theme="@style/Theme.Client" />
        <activity
            android:name="com.example.sundoboo.common.RendingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>